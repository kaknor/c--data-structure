version: 2
jobs:
  build:
    docker: 
      - image: debian:stretch 
    steps: 
      - checkout 
      - run: 
          name: make 
          command: make all
      - run: 
          name: Print the Current Time 
          command: date